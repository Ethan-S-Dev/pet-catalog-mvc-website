@model PetShop.Application.ViewModels.BestAnimalsViewModel
@{
    ViewBag.HomeClss = "active";
    ViewBag.Title = "Home";
}

<div>
    <div>
        <h2>Best two Pets</h2>
    </div>
    <div>
        @foreach (var animal in Model.Animals)
            {
                <partial name="AnimalView" model="animal" />
            }
    </div>
</div>
